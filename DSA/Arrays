#1.Minimum and Maximum element in the array

#include<iostream>
#include<climits>              //Library to use the INT_MIN and INT_MAX function
using namespace std;

int getMin(int num[],int n){

    int min=INT_MAX;

    for(int i=0;i<n;i++){
        if(num[i]<min){
            min=num[i];
        }
    }
    return min;
}

int getMax(int num[],int n){

    int max=INT_MIN;

    for(int i=0;i<n;i++){
        if(num[i]>max){
            max=num[i];
        }
    }
    return max;
}

int main(){
    int size;
    cout<<"Enter the size of the array"<<endl;
    cin>>size;

    int num[100];

    //Taking input in array
    for(int i=0;i<size;i++){
        cin>>num[i];
    }
}

/*Output:

Enter the size of the array
4
6
7
5
3
Maximum value is 7
Minimum value is 3*/


#2.Sum of all elements in an array

#include<iostream>
using namespace std;

int printSum(int num[],int n){
    int sum=0;
    for(int i=0;i<n;i++){
        sum=sum+num[i];
    }
    return sum;
}

int main(){

    int size;
    cout<<"Enter the size of an array"<<endl;
    cin>>size;

    int num[100];
    for(int i=0;i<size;i++){
        cin>>num[i];
    }
    cout<<"Sum of all elements in an array is "<<printSum(num,size);
}

/*Output:

Enter the size of an array
5
2
7
1
-4
11
Sum of all elements in an array is 17*/
